http://10.241.184.34:8075/report/queryBatchInfo

扫描当前项目的代码结构和业务逻辑

我主要负责css-bizrpt项目，你分析一下这个项目的代码结构和业务逻辑


现在需要新接入一个接口，是表格数据的接口，接口地址如下
/report/queryCondition

参数如下
{
  reportId: ""
}

返回结构如下：
{
    "result": [
        {
            "type": "1",
            "condition": "open settled balance",
            "selection": [
                {
                    "name": "all",
                    "idDefault": "Y"
                }
            ],
            "from": "11",
            "to": "22"
        }
    ],
    "errors": [],
    "warnings": [],
    "code": 1,
    "success": true,
    "message": ""
}

然后这个表格的第二列是一个下拉框，值是根据接口的selection来操作，这个也需要修改下

请实现



这个筛选条件一行一个太宽松了，改成一行两个试试


这个grid是不是要改一下，小屏幕下看不全页面








这个下拉框的默认参数是根据图中的isDefualt=Y来显示




现在需要新接入一个接口，是下载按钮的接口，接口地址如下
/report/export

参数如下
{
	
}

这些参数是根据页面上的参数来的，reportId、format、device、selections


返回结构如图二，是一个文件流，希望点击下载按钮后，能够把这个文件下载下来

这个文件名怎么有空格，把空格去掉一下


因为exportReportApi这个接口是下载文件，不知道文件有多大，所以希望timeout能够大一些


这个from和to需要修改一处逻辑，当从queryCondition获取数据后，有一个type字段，为1是下拉框，为2是条件看，当type=2的时候，同时selection下拉选中range的时候，form和to应该是一个input可以填写日期


不是日期输入框，而是普通的文本输入框

